[
  {
    "name": "/start sends welcome with main menu inline keyboard",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Welcome to the AGNTDEV bot!\n\nUse the menu below to navigate the bot's features."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "menu Status callback replies with operational status",
    "steps": [
      {
        "send": { "callback": "menu:status" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "sendMessage",
            "payload": { "text": "Bot is operational." }
          }
        ]
      }
    ]
  },
  {
    "name": "menu Help callback replies with available commands",
    "steps": [
      {
        "send": { "callback": "menu:help" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "sendMessage",
            "payload": { "text": "Available commands:\n/start — Show the main menu\n/status — Check bot operational status\n\nTap a menu button to navigate." }
          }
        ]
      }
    ]
  }
]
